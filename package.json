{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "serve": "php artisan serve",
        "dev:all": "concurrently -n laravel,vite -c cyan,green \"php artisan serve\" \"vite\"",
        "build": "vite build"
    },
    "devDependencies": {
        "axios": "^1.6.4",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^1.0.0",
        "vite": "^5.0.0"
    }
}
